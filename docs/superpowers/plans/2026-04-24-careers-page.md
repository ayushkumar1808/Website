# Careers Page Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a careers section to configai.co with 3 detailed job posting pages, an application form with file uploads, Supabase Storage for documents, and Resend email automation for both the admin and applicant.

**Architecture:** Static HTML pages on GitHub Pages. The browser JS uploads files directly to Supabase Storage, then POSTs applicant metadata to a new Deno edge function (`notify-application`) which inserts the DB row and fires two Resend emails. No build step — Tailwind CDN + Supabase JS CDN, same as the rest of the site.

**Tech Stack:** HTML, Tailwind CSS (CDN), Supabase JS SDK v2 (CDN), Supabase Storage, Supabase Edge Functions (Deno), Resend API, GitHub Pages

---

## File Map

| Action | File | Responsibility |
|---|---|---|
| Create | `careers.html` | Job listings hero + 3 role cards + EN/DE toggle |
| Create | `compiler-engineer.html` | Full job detail + sticky sidebar + application form |
| Create | `ml-engineer.html` | Full job detail + sticky sidebar + application form |
| Create | `hdl-engineer.html` | Full job detail + sticky sidebar + application form |
| Create | `thank-you.html` | Post-submission confirmation page |
| Create | `careers.js` | Shared: Supabase client init, form submission handler, file upload logic, EN/DE toggle |
| Create | `supabase/functions/notify-application/index.ts` | Edge function: signed URLs, DB insert, 2 Resend emails |
| Modify | `index.html` | Add Careers link to desktop nav, mobile menu, footer |
| Modify | `demo.html` | Add Careers link to desktop nav, mobile menu, footer |

---

## Task 1: Supabase — Create `applicants` Table with RLS

**Files:** Supabase dashboard SQL editor (no local files)

- [ ] **Step 1: Open Supabase SQL editor**

  Navigate to https://supabase.com/dashboard → project `wvatsmixbflbpugizpwu` → SQL Editor.

- [ ] **Step 2: Run the following SQL**

```sql
create table if not exists public.applicants (
  id                uuid primary key default gen_random_uuid(),
  name              text not null,
  email             text not null,
  role              text not null,
  role_slug         text not null,
  linkedin_url      text,
  cv_path           text not null,
  cover_letter_path text not null,
  additional_paths  jsonb default '[]'::jsonb,
  created_at        timestamptz default now()
);

alter table public.applicants enable row level security;

create policy "anon_insert_applicants"
  on public.applicants
  for insert
  to anon
  with check (true);
```

  Expected: "Success. No rows returned."

- [ ] **Step 3: Verify table exists**

  In Table Editor, confirm `applicants` appears with all 9 columns and RLS enabled (lock icon).

- [ ] **Step 4: Verify no SELECT for anon**

  In Auth > Policies for `applicants`, confirm only the `INSERT` policy exists — no SELECT/UPDATE/DELETE policies for anon.

---

## Task 2: Supabase — Create `applications` Storage Bucket with Policies

**Files:** Supabase dashboard Storage + SQL editor

- [ ] **Step 1: Create the bucket**

  Navigate to Storage → New bucket. Name: `applications`. **Toggle "Public bucket" OFF** (the dashboard defaults to public — make sure it is switched to private). Click Create.

  A private bucket means files have no public URLs. All access goes through signed URLs generated by the edge function. If you accidentally create it as public, delete it and recreate it as private before proceeding.

- [ ] **Step 2: Add anon upload policy via SQL editor**

```sql
create policy "anon_upload_applications"
  on storage.objects
  for insert
  to anon
  with check (bucket_id = 'applications');
```

  Expected: "Success. No rows returned."

- [ ] **Step 3: Verify policy**

  Storage → Policies → confirm `anon_upload_applications` insert policy exists on `storage.objects`.

---

## Task 3: Edge Function — `notify-application`

**Files:**
- Create: `supabase/functions/notify-application/index.ts`

- [ ] **Step 1: Create the file**

```typescript
import { serve } from 'https://deno.land/std@0.168.0/http/server.ts'
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'

const ALLOWED_ORIGINS = ['https://configai.co', 'https://www.configai.co']

function buildCorsHeaders(origin: string | null) {
  const allowed =
    origin &&
    (ALLOWED_ORIGINS.includes(origin) ||
      origin.startsWith('http://localhost') ||
      origin.startsWith('http://127.0.0.1'))
      ? origin
      : ALLOWED_ORIGINS[0]
  return {
    'Access-Control-Allow-Origin': allowed,
    'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
    'Access-Control-Allow-Methods': 'POST, OPTIONS',
  }
}

const ROLE_DISPLAY: Record<string, string> = {
  'compiler-engineer': 'Compiler Engineer',
  'ml-engineer': 'ML Engineer',
  'hdl-engineer': 'HDL Engineer',
}

function esc(s: string): string {
  return s
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;')
    .replace(/'/g, '&#39;')
}

serve(async (req) => {
  const origin = req.headers.get('origin')
  const cors = buildCorsHeaders(origin)

  if (req.method === 'OPTIONS') {
    return new Response('ok', { headers: cors })
  }

  try {
    const body = await req.json()
    const {
      name,
      email,
      role_slug,
      linkedin_url,
      cv_path,
      cover_letter_path,
      additional_paths = [],
    } = body

    if (!name || !email || !role_slug || !cv_path || !cover_letter_path) {
      return new Response(
        JSON.stringify({ error: 'Missing required fields' }),
        { status: 400, headers: { ...cors, 'Content-Type': 'application/json' } }
      )
    }

    const role = ROLE_DISPLAY[role_slug] ?? role_slug

    const supabase = createClient(
      Deno.env.get('SUPABASE_URL')!,
      Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!
    )

    // Generate 24-hour signed URLs (86400 seconds) for all uploaded files
    const allPaths: string[] = [cv_path, cover_letter_path, ...additional_paths]
    const signedResults = await Promise.all(
      allPaths.map((p) =>
        supabase.storage.from('applications').createSignedUrl(p, 86400)
      )
    )
    const [cvUrl, coverUrl, ...additionalUrls] = signedResults.map(
      (r) => r.data?.signedUrl ?? '(unavailable)'
    )

    // Insert DB row — paths are trusted (generated server-side from uploads)
    const { error: dbError } = await supabase.from('applicants').insert({
      name,
      email,
      role,
      role_slug,
      linkedin_url: linkedin_url || null,
      cv_path,
      cover_letter_path,
      additional_paths,
    })
    if (dbError) throw new Error(`DB insert failed: ${dbError.message}`)

    const resendKey = Deno.env.get('RESEND_API_KEY')!
    const ts = new Date().toLocaleString('en-US', { timeZone: 'Europe/Berlin' })

    const additionalLinksHtml =
      additionalUrls.length > 0
        ? additionalUrls
            .map((u, i) => `<li><a href="${u}">Additional document ${i + 1}</a></li>`)
            .join('')
        : '<li style="color:#aaa;">None submitted</li>'

    // Admin notification — user values are HTML-escaped before interpolation
    await fetch('https://api.resend.com/emails', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        Authorization: `Bearer ${resendKey}`,
      },
      body: JSON.stringify({
        from: 'ConfigAI Careers <careers@configai.co>',
        to: 'ayush@configai.co',
        subject: `New application: ${esc(name)} for ${esc(role)}`,
        html: `
<div style="font-family:sans-serif;max-width:520px;margin:0 auto;padding:32px;background:#f9f9f9;border-radius:8px;">
  <h2 style="margin:0 0 4px;font-size:18px;color:#0a0f18;">New application received</h2>
  <p style="margin:0 0 24px;color:#666;font-size:14px;">${esc(ts)} (Berlin time)</p>
  <table style="width:100%;border-collapse:collapse;">
    <tr><td style="padding:10px 0;border-bottom:1px solid #eee;font-size:13px;color:#999;width:120px;">Name</td><td style="padding:10px 0;border-bottom:1px solid #eee;font-size:14px;color:#0a0f18;">${esc(name)}</td></tr>
    <tr><td style="padding:10px 0;border-bottom:1px solid #eee;font-size:13px;color:#999;">Email</td><td style="padding:10px 0;border-bottom:1px solid #eee;font-size:14px;color:#0a0f18;"><a href="mailto:${esc(email)}">${esc(email)}</a></td></tr>
    <tr><td style="padding:10px 0;border-bottom:1px solid #eee;font-size:13px;color:#999;">Role</td><td style="padding:10px 0;border-bottom:1px solid #eee;font-size:14px;color:#0a0f18;">${esc(role)}</td></tr>
    <tr><td style="padding:10px 0;font-size:13px;color:#999;">LinkedIn</td><td style="padding:10px 0;font-size:14px;color:#0a0f18;">${linkedin_url ? `<a href="${esc(linkedin_url)}">${esc(linkedin_url)}</a>` : '&mdash;'}</td></tr>
  </table>
  <h3 style="margin:24px 0 12px;font-size:14px;color:#0a0f18;">Documents (links valid 24 hours)</h3>
  <ul style="padding-left:16px;font-size:14px;line-height:2;">
    <li><a href="${cvUrl}">CV / Resume</a></li>
    <li><a href="${coverUrl}">Cover Letter</a></li>
    ${additionalLinksHtml}
  </ul>
</div>`,
      }),
    })

    // Applicant confirmation — user values are HTML-escaped before interpolation
    await fetch('https://api.resend.com/emails', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        Authorization: `Bearer ${resendKey}`,
      },
      body: JSON.stringify({
        from: 'ConfigAI Careers <careers@configai.co>',
        to: email,
        subject: `We received your application for ${role} at ConfigAI`,
        html: `
<div style="font-family:sans-serif;max-width:520px;margin:0 auto;padding:32px;background:#f9f9f9;border-radius:8px;">
  <h2 style="margin:0 0 8px;font-size:18px;color:#0a0f18;">Application received</h2>
  <p style="margin:0 0 20px;color:#444;font-size:14px;line-height:1.7;">
    Hi ${esc(name)},<br><br>
    Thank you for applying for the <strong>${esc(role)}</strong> role at ConfigAI.
    We have received your application and will review it within 5 business days.<br><br>
    If you have any questions in the meantime, reach out at
    <a href="mailto:contact@configai.co">contact@configai.co</a>.
  </p>
  <p style="margin:0;color:#aaa;font-size:12px;">ConfigAI &middot; Saarbrucken, Germany</p>
</div>`,
      }),
    })

    return new Response(JSON.stringify({ success: true }), {
      status: 200,
      headers: { ...cors, 'Content-Type': 'application/json' },
    })
  } catch (err) {
    const msg = err instanceof Error ? err.message : String(err)
    return new Response(JSON.stringify({ error: msg }), {
      status: 500,
      headers: { ...cors, 'Content-Type': 'application/json' },
    })
  }
})
```

- [ ] **Step 2: Verify and set required secrets BEFORE deploying**

  The function will deploy silently even if secrets are missing, but fail at runtime. Set them first.

```bash
npx supabase secrets list --project-ref wvatsmixbflbpugizpwu
```

  `RESEND_API_KEY` should already appear (set by `notify-lead`). `SUPABASE_SERVICE_ROLE_KEY` and `SUPABASE_URL` are injected automatically by Supabase at runtime — do not set them manually.

  If `RESEND_API_KEY` is missing:
```bash
npx supabase secrets set RESEND_API_KEY=<your-key> --project-ref wvatsmixbflbpugizpwu
```

- [ ] **Step 3: Deploy the edge function**

```bash
cd /Users/ayushkumar/Website
npx supabase functions deploy notify-application --project-ref wvatsmixbflbpugizpwu
```

  Expected output includes: `Deployed notify-application`

- [ ] **Step 4: Smoke test the function with curl**

  Replace `<ANON_KEY>` with the key from `index.html`.

```bash
curl -s -X POST \
  'https://wvatsmixbflbpugizpwu.supabase.co/functions/v1/notify-application' \
  -H 'Authorization: Bearer <ANON_KEY>' \
  -H 'Content-Type: application/json' \
  -d '{"name":"","email":"","role_slug":"","cv_path":"","cover_letter_path":""}'
```

  Expected: `{"error":"Missing required fields"}` — confirms the function is live and reachable.

- [ ] **Step 5: Commit**

```bash
git add supabase/functions/notify-application/index.ts
git commit -m "feat: add notify-application edge function with signed URLs and Resend emails"
```

---

## Task 4: Create `careers.js` — Shared Utilities

**Files:**
- Create: `careers.js`

This file is loaded by all careers pages. It initialises the Supabase client, handles the EN/DE toggle, and manages the application form submission. All DOM updates use `textContent` (never `innerHTML`) to prevent XSS.

- [ ] **Step 1: Create `careers.js`**

```javascript
// ─── Supabase client ───────────────────────────────────────────────────────
const SUPABASE_URL = 'https://wvatsmixbflbpugizpwu.supabase.co'
const SUPABASE_ANON_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Ind2YXRzbWl4YmZsYnB1Z2l6cHd1Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzM0MzY5MzQsImV4cCI6MjA4OTAxMjkzNH0.SmaJx6z5VR9EhJBl0vnua1M8pmwEKl67WluGUDb9rDM'
const _sb = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY)

// ─── EN/DE Language Toggle ─────────────────────────────────────────────────
function applyLang(lang) {
  document.querySelectorAll('[data-en]').forEach(el => {
    el.textContent = lang === 'de' ? (el.dataset.de || el.dataset.en) : el.dataset.en
  })
  document.querySelectorAll('.lang-en').forEach(el => el.classList.toggle('hidden', lang !== 'en'))
  document.querySelectorAll('.lang-de').forEach(el => el.classList.toggle('hidden', lang !== 'de'))
  const btnEn = document.getElementById('lang-btn-en')
  const btnDe = document.getElementById('lang-btn-de')
  if (btnEn) btnEn.setAttribute('data-active', lang === 'en' ? 'true' : 'false')
  if (btnDe) btnDe.setAttribute('data-active', lang === 'de' ? 'true' : 'false')
  localStorage.setItem('lang', lang)
}

window.addEventListener('DOMContentLoaded', () => {
  applyLang(localStorage.getItem('lang') || 'en')
})

// ─── File Validation ───────────────────────────────────────────────────────
const CV_MIME = [
  'application/pdf',
  'application/msword',
  'application/vnd.openxmlformats-officedocument.wordprocessingml.document',
]
const ADDITIONAL_MIME = [...CV_MIME, 'application/zip', 'application/x-zip-compressed']
const MAX_CV_BYTES = 10 * 1024 * 1024
const MAX_ADDITIONAL_BYTES = 20 * 1024 * 1024

function validateFile(file, allowedTypes, maxBytes) {
  if (!allowedTypes.includes(file.type)) return 'Invalid file type'
  if (file.size > maxBytes) return `File exceeds ${maxBytes / 1024 / 1024} MB limit`
  return null
}

// ─── Application Form Submission ───────────────────────────────────────────
async function submitApplication(event) {
  event.preventDefault()
  const form = event.target
  const btn = form.querySelector('[data-submit-btn]')
  const errorEl = form.querySelector('[data-form-error]')

  const roleSlug = form.dataset.roleSlug
  const name = form.querySelector('[name=name]').value.trim()
  const email = form.querySelector('[name=email]').value.trim()
  const linkedinUrl = form.querySelector('[name=linkedin_url]').value.trim()
  const cvFile = form.querySelector('[name=cv]').files[0]
  const coverFile = form.querySelector('[name=cover_letter]').files[0]
  const additionalFiles = [...(form.querySelector('[name=additional]').files || [])]

  // Clear previous error using textContent (safe, no XSS risk)
  errorEl.textContent = ''

  if (!cvFile) return showError(errorEl, btn, 'Please upload your CV.')
  const cvErr = validateFile(cvFile, CV_MIME, MAX_CV_BYTES)
  if (cvErr) return showError(errorEl, btn, 'CV: ' + cvErr)

  if (!coverFile) return showError(errorEl, btn, 'Please upload your cover letter.')
  const coverErr = validateFile(coverFile, CV_MIME, MAX_CV_BYTES)
  if (coverErr) return showError(errorEl, btn, 'Cover letter: ' + coverErr)

  for (const f of additionalFiles) {
    const err = validateFile(f, ADDITIONAL_MIME, MAX_ADDITIONAL_BYTES)
    if (err) return showError(errorEl, btn, f.name + ': ' + err)
  }

  setBtn(btn, 'Uploading files...')

  try {
    const applicantId = crypto.randomUUID()
    const folder = roleSlug + '/' + applicantId

    const cvPath = folder + '/cv' + extOf(cvFile.name)
    const { error: e1 } = await _sb.storage.from('applications').upload(cvPath, cvFile)
    if (e1) throw new Error('CV upload failed: ' + e1.message)

    const coverPath = folder + '/cover-letter' + extOf(coverFile.name)
    const { error: e2 } = await _sb.storage.from('applications').upload(coverPath, coverFile)
    if (e2) throw new Error('Cover letter upload failed: ' + e2.message)

    const additionalPaths = []
    for (let i = 0; i < additionalFiles.length; i++) {
      const f = additionalFiles[i]
      const p = folder + '/additional/' + i + '-' + f.name.replace(/[^a-zA-Z0-9._-]/g, '_')
      const { error: e3 } = await _sb.storage.from('applications').upload(p, f)
      if (e3) throw new Error(f.name + ' upload failed: ' + e3.message)
      additionalPaths.push(p)
    }

    setBtn(btn, 'Submitting...')

    const res = await fetch(SUPABASE_URL + '/functions/v1/notify-application', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': 'Bearer ' + SUPABASE_ANON_KEY,
      },
      body: JSON.stringify({
        name, email, role_slug: roleSlug, linkedin_url: linkedinUrl,
        cv_path: cvPath, cover_letter_path: coverPath, additional_paths: additionalPaths,
      }),
    })

    if (!res.ok) {
      const data = await res.json().catch(() => ({}))
      throw new Error(data.error || 'Server error ' + res.status)
    }

    window.location.href = 'thank-you.html'
  } catch (err) {
    showError(errorEl, btn, err.message)
  }
}

function extOf(filename) {
  const i = filename.lastIndexOf('.')
  return i >= 0 ? filename.slice(i) : ''
}

function setBtn(btn, text) {
  btn.textContent = text
  btn.disabled = true
}

function showError(errorEl, btn, msg) {
  errorEl.textContent = msg   // textContent is safe — no HTML parsing
  btn.textContent = 'Submit Application'
  btn.disabled = false
}

window.addEventListener('DOMContentLoaded', () => {
  const form = document.getElementById('application-form')
  if (form) form.addEventListener('submit', submitApplication)

  // Show filename when file is selected
  document.querySelectorAll('.upload-zone input[type=file]').forEach(input => {
    input.addEventListener('change', () => {
      const nameEl = input.closest('.upload-zone').querySelector('.file-name')
      if (nameEl) nameEl.textContent = input.files[0]?.name || ''
    })
  })
})
```

- [ ] **Step 2: Commit**

```bash
git add careers.js
git commit -m "feat: add careers.js shared utilities (lang toggle, file upload, form submission)"
```

---

## Task 5: `careers.html` — Job Listings Page

**Files:**
- Create: `careers.html`

- [ ] **Step 1: Create `careers.html`**

  Use `demo.html` as the structural template (separate page, no snap-scroll). Key details:

  **`<head>`:** Title `Careers · ConfigAI`. Same meta, fonts, Tailwind CDN, Supabase JS CDN as `index.html`. Add `<script src="careers.js" defer></script>`.

  **Nav** (same pattern as `demo.html`): logo → `index.html`, centre links (Challenge/Platform → `index.html`, Careers → `careers.html` with active underline style `border-bottom: 1px solid rgba(255,255,255,0.4)`). Add EN/DE toggle in right-side nav:

  ```html
  <div class="hidden md:flex items-center gap-1 text-xs font-semibold mr-1">
    <button id="lang-btn-en" onclick="applyLang('en')"
      data-active="true"
      class="px-2 py-1 rounded transition-colors text-white"
      style="background:rgba(255,255,255,0.1);">EN</button>
    <span class="text-zinc-600">/</span>
    <button id="lang-btn-de" onclick="applyLang('de')"
      data-active="false"
      class="px-2 py-1 rounded transition-colors text-zinc-400 hover:text-white">DE</button>
  </div>
  ```

  Use CSS attribute selector to style the active button:
  ```html
  <style>
    [data-active="true"] { color: #fff !important; background: rgba(255,255,255,0.12) !important; }
    [data-active="false"] { color: rgba(255,255,255,0.45); }
  </style>
  ```

  **Hero section:**
  ```html
  <section class="text-center px-6 pt-28 pb-16">
    <p class="text-xs uppercase tracking-[0.2em] text-zinc-400 mb-4 font-light"
       data-en="Careers at ConfigAI" data-de="Karriere bei ConfigAI">Careers at ConfigAI</p>
    <h1 class="font-light text-white text-4xl md:text-5xl leading-tight mb-5"
        style="font-family:'Playfair Display',serif;"
        data-en="Build the future of AI hardware."
        data-de="Gestalte die Zukunft der KI-Hardware.">
      Build the future of AI hardware.
    </h1>
    <p class="text-base text-zinc-300 font-light max-w-xl mx-auto leading-relaxed"
       data-en="We're a small team solving a hard problem: turning ML models into FPGA hardware automatically. Based in Saarbrucken, backed by Max Planck Institute for Informatics and Google for Startups."
       data-de="Wir sind ein kleines Team, das ein schwieriges Problem lost: ML-Modelle werden automatisch in FPGA-Hardware umgewandelt. Ansassig in Saarbrucken, unterstutzt vom Max-Planck-Institut fur Informatik und Google for Startups.">
      We're a small team solving a hard problem: turning ML models into FPGA hardware automatically. Based in Saarbrucken, backed by Max Planck Institute for Informatics and Google for Startups.
    </p>
  </section>
  ```

  **Job cards grid:**
  ```html
  <section class="px-6 pb-24">
    <div class="max-w-5xl mx-auto">
      <p class="text-xs uppercase tracking-[0.16em] text-zinc-500 mb-6"
         data-en="Open Positions · 3 roles" data-de="Offene Stellen · 3 Rollen">Open Positions · 3 roles</p>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-5">

        <!-- Card 1: Compiler Engineer -->
        <div class="rounded-2xl p-6 flex flex-col gap-4"
             style="background:rgba(13,18,31,0.8);border:1px solid rgba(255,255,255,0.06);">
          <!-- SVG icon: </> -->
          <div class="w-9 h-9 rounded-lg flex items-center justify-center flex-shrink-0"
               style="background:rgba(255,255,255,0.05);">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none"
                 stroke="rgba(255,255,255,0.5)" stroke-width="1.8">
              <polyline points="16 18 22 12 16 6"/><polyline points="8 6 2 12 8 18"/>
            </svg>
          </div>
          <div>
            <h2 class="text-sm font-semibold text-white mb-2"
                data-en="Compiler Engineer" data-de="Compiler Engineer">Compiler Engineer</h2>
            <div class="flex flex-col gap-1.5 text-xs text-zinc-400">
              <span data-en="15 hrs / week" data-de="15 Std. / Woche">15 hrs / week</span>
              <span data-en="On-site · Saarbrucken" data-de="Vor Ort · Saarbrucken">On-site · Saarbrucken</span>
              <span data-en="Part-time · m/w/d" data-de="Teilzeit · m/w/d">Part-time · m/w/d</span>
            </div>
          </div>
          <p class="text-xs text-zinc-400 leading-relaxed flex-1"
             data-en="Work on the compiler pipeline that maps ML computation graphs to FPGA primitives: parsing, IR transformations and code generation."
             data-de="Arbeit an der Compiler-Pipeline, die ML-Berechnungsgraphen auf FPGA-Primitive abbildet: Parsing, IR-Transformationen und Code-Generierung.">
            Work on the compiler pipeline that maps ML computation graphs to FPGA primitives: parsing, IR transformations and code generation.
          </p>
          <a href="compiler-engineer.html"
             class="w-full text-center text-xs font-semibold uppercase tracking-widest py-2.5 rounded-lg transition-colors block"
             style="background:rgba(255,255,255,0.06);border:1px solid rgba(255,255,255,0.1);color:rgba(255,255,255,0.75);"
             data-en="View role" data-de="Stelle ansehen">View role</a>
        </div>

        <!-- Card 2: ML Engineer — same structure, href="ml-engineer.html" -->
        <!-- Card 3: HDL Engineer — same structure, href="hdl-engineer.html" -->

      </div>
    </div>
  </section>
  ```

  Repeat the card markup for ML Engineer (icon: radial/star SVG, description from spec) and HDL Engineer (icon: chip SVG, description from spec).

  **Footer:** Same as `demo.html` footer, add `<a href="careers.html">Careers</a>` to the link row.

- [ ] **Step 2: Verify in browser**

  `python3 -m http.server 8080` then open `http://localhost:8080/careers.html`. Check:
  - 3 cards visible with correct titles, meta and descriptions
  - EN/DE toggle switches all `data-en`/`data-de` text, active button highlights
  - Language persists on reload
  - "View role" links navigate to correct pages

- [ ] **Step 3: Commit**

```bash
git add careers.html
git commit -m "feat: add careers.html job listings page"
```

---

## Task 6: `compiler-engineer.html` — Full Job Detail Page

**Files:**
- Create: `compiler-engineer.html`

- [ ] **Step 1: Create `compiler-engineer.html`**

  **`<head>`:** Title `Compiler Engineer · Careers · ConfigAI`. Same deps + `<script src="careers.js" defer></script>`.

  **Nav:** Same as `careers.html` (Careers active).

  **Breadcrumb** (below nav, `padding-top: 52px` to clear fixed nav):
  ```html
  <nav class="max-w-5xl mx-auto px-6 lg:px-10 py-4 text-xs text-zinc-500 flex gap-2">
    <a href="careers.html" class="hover:text-white transition-colors"
       data-en="Careers" data-de="Karriere">Careers</a>
    <span>›</span>
    <span class="text-zinc-300"
          data-en="Compiler Engineer" data-de="Compiler Engineer">Compiler Engineer</span>
  </nav>
  ```

  **Two-column grid** (`<div class="max-w-5xl mx-auto px-6 lg:px-10 py-8 grid grid-cols-1 lg:grid-cols-[1fr_240px] gap-12 items-start">`):

  **Left column content** (use copy from spec Section 6 — Compiler Engineer):
  - `<h1>` in Playfair Display, font-light, text-4xl
  - 6 sections each in `<div class="mb-10">`: About ConfigAI, About the Role, Key Responsibilities (ul), Required Skills (ul), Preferred Skills (ul), Why Join Us (ul)
  - All section headings use `data-en`/`data-de`; body text in `<p>` or `<li>` also uses `data-en`/`data-de` attributes
  - `<strong>` tags on key terms within body copy
  - No em dashes anywhere — use colons or middots

  **Right column sidebar** (`<aside class="lg:sticky lg:top-20 pl-0 lg:pl-8 border-l-0 lg:border-l lg:border-white/5">`):
  ```html
  <!-- Meta items -->
  <div class="flex flex-col gap-5 mb-6">
    <div class="flex gap-3 items-start">
      <!-- briefcase icon -->
      <div class="flex-col">
        <p class="text-xs text-zinc-500 uppercase tracking-wider mb-0.5"
           data-en="Department" data-de="Abteilung">Department</p>
        <p class="text-sm text-zinc-200 font-medium"
           data-en="Engineering" data-de="Engineering">Engineering</p>
      </div>
    </div>
    <!-- Location, Commitment, Type — same pattern -->
  </div>

  <!-- Apply Now -->
  <a href="#apply"
     class="block w-full text-center bg-white text-[#0a0f18] font-bold text-xs
            uppercase tracking-widest py-3 rounded-lg hover:bg-zinc-100 transition-colors mb-4"
     data-en="Apply Now" data-de="Jetzt bewerben">Apply Now</a>

  <!-- Share row -->
  <p class="text-xs text-zinc-500 uppercase tracking-wider mb-2"
     data-en="Share" data-de="Teilen">Share</p>
  <div class="flex gap-2">
    <!-- LinkedIn icon button linking to linkedin.com/shareArticle -->
    <!-- X icon button linking to x.com/intent/tweet -->
  </div>
  ```

  **Application form section** (full width, below the grid):
  ```html
  <section id="apply" class="max-w-5xl mx-auto px-6 lg:px-10 pb-24 scroll-mt-20">
    <div class="border-t border-white/6 pt-10 mb-8">
      <h2 class="font-light text-2xl text-white mb-2"
          style="font-family:'Playfair Display',serif;"
          data-en="Submit your application" data-de="Bewerbung einreichen">
        Submit your application
      </h2>
      <p class="text-sm text-zinc-400"
         data-en="CV and cover letter required. Files stored securely."
         data-de="Lebenslauf und Anschreiben erforderlich.">
        CV and cover letter required. Files stored securely.
      </p>
    </div>

    <form id="application-form" data-role-slug="compiler-engineer" novalidate>

      <!-- Row 1: Name + Email -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-4">
        <div>
          <label class="block text-xs uppercase tracking-widest text-zinc-400 mb-2 font-semibold"
                 data-en="Full Name *" data-de="Vollstandiger Name *">Full Name *</label>
          <input type="text" name="name" required placeholder="Jane Smith"
            class="w-full px-3 py-2.5 text-sm text-white rounded-lg outline-none transition-colors"
            style="background:rgba(255,255,255,0.04);border:1px solid rgba(255,255,255,0.08);"
            onfocus="this.style.borderColor='rgba(255,255,255,0.25)'"
            onblur="this.style.borderColor='rgba(255,255,255,0.08)'">
        </div>
        <div>
          <label class="block text-xs uppercase tracking-widest text-zinc-400 mb-2 font-semibold"
                 data-en="Email *" data-de="E-Mail *">Email *</label>
          <input type="email" name="email" required placeholder="jane@example.com"
            class="w-full px-3 py-2.5 text-sm text-white rounded-lg outline-none transition-colors"
            style="background:rgba(255,255,255,0.04);border:1px solid rgba(255,255,255,0.08);"
            onfocus="this.style.borderColor='rgba(255,255,255,0.25)'"
            onblur="this.style.borderColor='rgba(255,255,255,0.08)'">
        </div>
      </div>

      <!-- Row 2: Applying For (locked) + LinkedIn -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-6">
        <div>
          <label class="block text-xs uppercase tracking-widest text-zinc-400 mb-2 font-semibold"
                 data-en="Applying For" data-de="Bewerbung fur">Applying For</label>
          <div class="w-full px-3 py-2.5 text-sm text-zinc-400 rounded-lg flex items-center justify-between"
               style="background:rgba(255,255,255,0.04);border:1px solid rgba(255,255,255,0.08);">
            <span data-en="Compiler Engineer" data-de="Compiler Engineer">Compiler Engineer</span>
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none"
                 stroke="rgba(255,255,255,0.2)" stroke-width="2">
              <rect x="3" y="11" width="18" height="11" rx="2"/>
              <path d="M7 11V7a5 5 0 0110 0v4"/>
            </svg>
          </div>
        </div>
        <div>
          <label class="block text-xs uppercase tracking-widest text-zinc-400 mb-2 font-semibold"
                 data-en="LinkedIn Profile" data-de="LinkedIn-Profil">LinkedIn Profile</label>
          <input type="url" name="linkedin_url" placeholder="https://linkedin.com/in/..."
            class="w-full px-3 py-2.5 text-sm text-white rounded-lg outline-none transition-colors"
            style="background:rgba(255,255,255,0.04);border:1px solid rgba(255,255,255,0.08);"
            onfocus="this.style.borderColor='rgba(255,255,255,0.25)'"
            onblur="this.style.borderColor='rgba(255,255,255,0.08)'">
        </div>
      </div>

      <!-- Upload zones -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6">

        <!-- CV (required) -->
        <label class="upload-zone block cursor-pointer rounded-xl p-5 text-center transition-colors"
               style="border:1.5px dashed rgba(255,255,255,0.15);">
          <input type="file" name="cv" accept=".pdf,.doc,.docx" class="sr-only">
          <svg class="mx-auto mb-2" width="20" height="20" viewBox="0 0 24 24" fill="none"
               stroke="rgba(255,255,255,0.35)" stroke-width="1.8">
            <path d="M21 15v4a2 2 0 01-2 2H5a2 2 0 01-2-2v-4"/>
            <polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/>
          </svg>
          <p class="text-xs font-semibold text-zinc-300 mb-1"
             data-en="CV / Resume" data-de="Lebenslauf">CV / Resume</p>
          <p class="text-xs text-zinc-500 mb-2"
             data-en="PDF or DOC · max 10 MB" data-de="PDF oder DOC · max 10 MB">PDF or DOC · max 10 MB</p>
          <span class="text-xs px-2 py-0.5 rounded"
                style="background:rgba(255,255,255,0.1);color:rgba(255,255,255,0.5);"
                data-en="REQUIRED" data-de="PFLICHTFELD">REQUIRED</span>
          <span class="file-name block text-xs text-zinc-400 mt-2 truncate"></span>
        </label>

        <!-- Cover Letter (required) — same structure, name="cover_letter" -->

        <!-- Additional Docs (optional) — same structure, name="additional", accept=".pdf,.doc,.docx,.zip", OPTIONAL badge -->

      </div>

      <!-- Error + submit -->
      <p data-form-error class="text-sm text-red-400 min-h-[20px] mb-3"></p>
      <div class="flex justify-end">
        <button type="submit" data-submit-btn
          class="bg-white text-[#0a0f18] font-bold text-xs uppercase tracking-widest
                 px-8 py-3 rounded-lg hover:bg-zinc-100 transition-colors disabled:opacity-50 disabled:cursor-not-allowed"
          data-en="Submit Application" data-de="Bewerbung absenden">
          Submit Application
        </button>
      </div>

    </form>
  </section>
  ```

  **Footer:** Same as `careers.html`.

- [ ] **Step 2: Verify in browser**

  - "Apply Now" scrolls to `#apply`
  - Clicking upload zone opens file picker; selected filename appears
  - Submitting with no files shows "Please upload your CV."
  - Wrong file type (`.jpg`) shows "Invalid file type"
  - EN/DE toggle switches all translated elements

- [ ] **Step 3: Commit**

```bash
git add compiler-engineer.html
git commit -m "feat: add compiler-engineer job detail page with application form"
```

---

## Task 7: `ml-engineer.html` — ML Engineer Detail Page

**Files:**
- Create: `ml-engineer.html`

- [ ] **Step 1: Create `ml-engineer.html`** — identical structure to `compiler-engineer.html` with:

  | Field | Value |
  |---|---|
  | `<title>` | `ML Engineer · Careers · ConfigAI` |
  | Breadcrumb label | `ML Engineer` |
  | `<h1>` | `ML Engineer` |
  | `data-role-slug` on form | `ml-engineer` |
  | All body copy | ML Engineer section from spec Section 6 |
  | Sidebar role | `ML Engineer` |
  | Share URL slug | `ml-engineer.html` |

- [ ] **Step 2: Verify in browser** — same checklist as Task 6 Step 2.

- [ ] **Step 3: Commit**

```bash
git add ml-engineer.html
git commit -m "feat: add ml-engineer job detail page with application form"
```

---

## Task 8: `hdl-engineer.html` — HDL Engineer Detail Page

**Files:**
- Create: `hdl-engineer.html`

- [ ] **Step 1: Create `hdl-engineer.html`** — identical structure with:

  | Field | Value |
  |---|---|
  | `<title>` | `HDL Engineer · Careers · ConfigAI` |
  | Breadcrumb label | `HDL Engineer` |
  | `<h1>` | `HDL Engineer` |
  | `data-role-slug` on form | `hdl-engineer` |
  | All body copy | HDL Engineer section from spec Section 6 |
  | Sidebar role | `HDL Engineer` |
  | Share URL slug | `hdl-engineer.html` |

- [ ] **Step 2: Verify in browser** — same checklist as Task 6 Step 2.

- [ ] **Step 3: Commit**

```bash
git add hdl-engineer.html
git commit -m "feat: add hdl-engineer job detail page with application form"
```

---

## Task 9: `thank-you.html` — Post-Submission Page

**Files:**
- Create: `thank-you.html`

- [ ] **Step 1: Create `thank-you.html`**

  Same nav/footer as `careers.html`. No Supabase calls. `<script src="careers.js" defer></script>` for lang toggle only.

  **Main content** (flex-1, centred):
  ```html
  <main class="flex-1 flex flex-col items-center justify-center text-center px-6 py-32">
    <div class="max-w-md mx-auto">
      <div class="w-12 h-12 rounded-full flex items-center justify-center mx-auto mb-6"
           style="background:rgba(255,255,255,0.06);border:1px solid rgba(255,255,255,0.1);">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none"
             stroke="rgba(255,255,255,0.6)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <polyline points="20 6 9 17 4 12"/>
        </svg>
      </div>
      <h1 class="font-light text-white text-4xl mb-4"
          style="font-family:'Playfair Display',serif;"
          data-en="Application received." data-de="Bewerbung eingegangen.">
        Application received.
      </h1>
      <p class="text-zinc-300 font-light leading-relaxed mb-8 text-sm"
         data-en="Thank you for applying to ConfigAI. We have sent a confirmation to your email address. We will review your application and get back to you within 5 business days. In the meantime, feel free to reach out at contact@configai.co."
         data-de="Vielen Dank fur Ihre Bewerbung bei ConfigAI. Wir haben eine Bestatigung an Ihre E-Mail-Adresse gesendet. Wir werden Ihre Bewerbung prufen und uns innerhalb von 5 Werktagen melden.">
        Thank you for applying to ConfigAI. We have sent a confirmation to your email address. We will review your application and get back to you within 5 business days. In the meantime, feel free to reach out at contact@configai.co.
      </p>
      <div class="flex gap-4 justify-center text-sm">
        <a href="careers.html" class="text-zinc-300 hover:text-white transition-colors underline underline-offset-4"
           data-en="View open roles" data-de="Offene Stellen ansehen">View open roles</a>
        <span class="text-zinc-600">·</span>
        <a href="index.html" class="text-zinc-300 hover:text-white transition-colors underline underline-offset-4"
           data-en="Back to home" data-de="Zur Startseite">Back to home</a>
      </div>
    </div>
  </main>
  ```

- [ ] **Step 2: Verify in browser** — page renders, EN/DE toggle works, both links navigate correctly.

- [ ] **Step 3: Commit**

```bash
git add thank-you.html
git commit -m "feat: add thank-you post-submission page"
```

---

## Task 10: Update Nav + Footer in `index.html` and `demo.html`

**Files:**
- Modify: `index.html`
- Modify: `demo.html`

### `index.html`

- [ ] **Step 1: Add "Careers" to desktop nav centre links**

  In `index.html`, find the `<div class="hidden md:flex absolute left-1/2 ...">` nav centre div. Add after the last `<a>`:
  ```html
  <a href="careers.html"
     class="text-sm font-medium text-zinc-300 hover:text-white transition-colors">Careers</a>
  ```

- [ ] **Step 2: Add "Careers" to mobile menu links**

  In the `<div class="flex flex-col px-6 pt-4">` mobile nav, add before the `<div class="border-t ...">` separator:
  ```html
  <a href="careers.html"
     class="text-white text-base font-normal py-[9px] hover:text-zinc-300 transition-colors">Careers</a>
  ```

- [ ] **Step 3: Add "Careers" to footer**

  In the footer link row `<div class="flex flex-wrap items-center gap-4 ...">`, add after the Blog link:
  ```html
  <a href="careers.html" class="hover:text-white transition-colors">Careers</a>
  ```

- [ ] **Step 4: Add EN/DE toggle and load careers.js**

  In the desktop nav right-side `<div class="hidden md:flex items-center gap-3">`, add before the "Try Demo" link:
  ```html
  <div class="flex items-center gap-1 text-xs font-semibold">
    <button id="lang-btn-en" onclick="applyLang('en')" data-active="true"
      class="px-2 py-1 rounded text-white" style="background:rgba(255,255,255,0.1);">EN</button>
    <span class="text-zinc-600">/</span>
    <button id="lang-btn-de" onclick="applyLang('de')" data-active="false"
      class="px-2 py-1 rounded text-zinc-400 hover:text-white transition-colors">DE</button>
  </div>
  ```

  Add before `</body>`:
  ```html
  <script src="careers.js" defer></script>
  ```

### `demo.html`

- [ ] **Step 5: Add "Careers" to `demo.html` desktop nav** — same link as Step 1 added to the centre `<div class="hidden md:flex ...">`.

- [ ] **Step 6: Add "Careers" to `demo.html` mobile menu** — after the existing `<a href="demo.html">` link and before the border separator.

- [ ] **Step 7: Add "Careers" to `demo.html` footer** — in the `<div class="flex flex-wrap ...">` link row.

- [ ] **Step 8: Add EN/DE toggle to `demo.html`** — same snippet as Step 4. Add `<script src="careers.js" defer></script>` before `</body>`.

- [ ] **Step 9: Commit**

```bash
git add index.html demo.html
git commit -m "feat: add Careers link and EN/DE toggle to index.html and demo.html"
```

---

## Task 11: End-to-End Smoke Test

- [ ] **Step 1: Serve site locally**

```bash
cd /Users/ayushkumar/Website
python3 -m http.server 8080
```

  Open `http://localhost:8080`.

- [ ] **Step 2: Check index.html and demo.html**

  Careers link visible in desktop nav, mobile menu, footer. EN/DE toggle appears and switches text. Language persists on page reload.

- [ ] **Step 3: Check careers.html**

  3 cards render. EN/DE toggle works. "View role" links navigate correctly.

- [ ] **Step 4: Check a job detail page**

  - All 6 content sections present with correct copy from spec
  - Sidebar is sticky on desktop
  - "Apply Now" smooth-scrolls to `#apply` (`scroll-mt-20` clears the fixed nav)
  - Clicking upload zone opens file picker; filename shows after selection
  - Submitting empty form: "Please upload your CV."
  - Wrong MIME type: "Invalid file type"
  - File over 10 MB: "File exceeds 10 MB limit"

- [ ] **Step 5: Submit a real test application**

  Use real PDFs. Use `ayush@configai.co` as the applicant email so both emails go to you.

  Expected:
  - Button: "Uploading files..." → "Submitting..." → redirect to `thank-you.html`
  - Admin email at `ayush@configai.co`: subject "New application: [Name] for Compiler Engineer", table with applicant details, signed download links for CV and cover letter (valid 24 hours)
  - Confirmation email at the submitted address: subject "We received your application for Compiler Engineer at ConfigAI"
  - Row in `applicants` table in Supabase dashboard
  - Files in `applications/compiler-engineer/<uuid>/` in Supabase Storage

- [ ] **Step 6: Check thank-you.html**

  Renders cleanly. Both links work. EN/DE toggle works.

- [ ] **Step 6b: If emails don't arrive, check edge function logs**

```bash
npx supabase functions logs notify-application --project-ref wvatsmixbflbpugizpwu
```

  Common failures: `RESEND_API_KEY` missing (secret not set), CORS origin mismatch (check `Origin` header vs `ALLOWED_ORIGINS`), DB insert RLS error (anon insert policy not created). Logs show the exact error.

- [ ] **Step 7: Push to GitHub Pages**

```bash
git push origin main
```

  Wait ~2 minutes then verify `https://configai.co/careers.html` is live.

- [ ] **Step 8: Verify Resend sender domain**

  Before going live, confirm `careers@configai.co` is a verified sender in the Resend dashboard (Settings → Domains). If not, add and verify `configai.co` as a sending domain or use an already-verified address.

---

## Reference

- Spec: `docs/superpowers/specs/2026-04-24-careers-page-design.md`
- Supabase project: `wvatsmixbflbpugizpwu`
- Existing edge function pattern: `supabase/functions/notify-lead/index.ts`
- Resend from address: `careers@configai.co` (must be verified domain in Resend)
- Admin email: `ayush@configai.co`
- Anon key: in `index.html` line 1298 (`_sb` initialisation)
